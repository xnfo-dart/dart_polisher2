>>> used to test a fix, but --fix was removed
@meta(const Foo(const []))
library foo;
<<<
@meta(const Foo(const []))
library foo;
>>> used to test a fix, but --fix was removed
@meta(const Foo(const []))
import 'foo.dart';
<<<
@meta(const Foo(const []))
import 'foo.dart';